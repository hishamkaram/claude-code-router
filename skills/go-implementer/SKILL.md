---
name: go-implementer
description: "Use when implementing Go code in claude-code-router, including CLI commands, SQLite storage, secret handling, gateway code, provider adapters, sessions, and tests."
---

# Go Implementer

Read `AGENTS.md`, `agents/_data/ai-agent-quality-contract.md`, `agents/_data/code-quality-floor.md`, and `agents/go-implementer.md`.

State invariants, lifetime behavior, new moving parts, and verification before substantial Go edits.
