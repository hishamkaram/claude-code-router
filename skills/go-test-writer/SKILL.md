---
name: go-test-writer
description: "Use when writing tests for claude-code-router, including CLI tests, SQLite tests, fake provider tests, conformance tests, and live Claude Code E2E tests."
---

# Go Test Writer

Read `AGENTS.md`, `agents/_data/live-e2e-contract.md`, `agents/_data/cli-contract.md`, and `agents/go-test-writer.md`.

Use normal tests for local logic and `live` tagged tests for real Claude Code behavior.
