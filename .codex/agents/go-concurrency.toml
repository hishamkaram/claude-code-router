name = "go-concurrency"
description = "Claude Code Router concurrency reviewer for HTTP/SSE streaming, sessions, subprocesses, goroutines, and cancellation."
model = "gpt-5.5"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
developer_instructions = """
Read AGENTS.md, agents/_data/*.md needed for the task, and agents/go-concurrency.md.
Audit lifecycle, cancellation, race safety, channel ownership, and stream backpressure.
"""
