# CCR v0.4.5 Acceptance Criteria

This release fixes OpenAI-compatible handling of Anthropic assistant text
blocks with citation metadata. A checked item requires reproducible evidence on
the final release candidate; skipped live tests are not evidence.

## Behavior

- [x] Assistant text blocks with `citations` are accepted on the OpenAI Chat
  Completions conversion path.
- [x] Assistant text is preserved, while unsupported citation metadata is
  omitted from the downstream provider request.
- [x] The existing compatibility response header reports the exact ignored
  field marker `messages[].content[].citations`.
- [x] Multiple ignored-field markers are de-duplicated without hiding other
  compatibility degradation markers.
- [x] Unknown assistant fields remain rejected, as do citations or unknown
  fields in user, system, and tool-result text blocks.
- [x] Anthropic Responses conversion accepts citation-bearing assistant blocks
  and preserves their text.
- [x] A live Claude Code model switch proves citation-bearing assistant history
  no longer produces a 501 unsupported-content error.

## Privacy and Security

- [x] Citation contents are not forwarded to OpenAI-compatible providers after
  conversion; only the field-name marker is exposed in the response header.
- [x] No provider secret handling, persistence, or logging path was changed.

## Required Verification

- [x] `go test ./...`
- [x] `go test -race -count=1 -p 4 ./...`
- [x] `go vet ./...`
- [x] `golangci-lint run ./...`
- [x] `GOTOOLCHAIN=go1.25.13+auto govulncheck ./...` reports no vulnerabilities.
- [x] `go test -tags=live -count=1 -p 1 ./...`
- [x] Focused live citation fixture passes with the installed Claude Code CLI.
- [x] `goreleaser check`
- [x] `goreleaser release --snapshot --clean`
- [x] `actionlint`
- [x] `git diff --check`
- [x] The exact uncommitted Codex review reports no findings:

  ```bash
  codex review -c 'sandbox_mode="danger-full-access"' \
    -c 'approval_policy="never"' \
    -c 'mcp_servers.playwright.enabled=false' \
    -c 'mcp_servers.chrome-devtools.enabled=false' \
    --uncommitted
  ```

- [x] Hosted pull-request CI and the Live Claude Code E2E workflows pass with
  no required-job skips.

## Release

- [x] The complete patch is squash-merged through the reviewed PR with green
  CI and the feature branch is deleted.
- [x] Annotated tag `v0.4.5` points to the approved commit reachable from
  `main`.
- [x] Draft release archives, checksums, provenance, and the signed GHCR
  browser image are verified.
- [x] Manual promotion publishes the GitHub Release and updates Homebrew.

## Evidence

Verified locally on 2026-08-14 for the release candidate:

- The unit, race, vet, lint, and focused gateway integration tests passed.
- `go test -tags=live -count=1 -p 1 ./...` passed, including the live Claude
  Code citation model-switch fixture.
- Go 1.25.13 was selected because the prior repository pin, Go 1.25.12,
  exposed standard-library vulnerabilities in `govulncheck`; the patched
  toolchain scan passed with zero findings.
- The exact requested uncommitted Codex review reported no discrete
  correctness issues.
- PR #49 was squash-merged as
  `466dcdb0384ae886f4250ff62f32bc2421fb72f1`, and its remote feature branch was
  deleted.
- Hosted PR Test run
  [31770755780](https://github.com/hishamkaram/claude-code-router/actions/runs/31770755780)
  passed. The 12-job fixture matrix, Docker CUA fixture, and macOS helper passed
  in [run 31770755758](https://github.com/hishamkaram/claude-code-router/actions/runs/31770755758)
  without required-job skips.
- PR #50 recorded the pre-tag evidence and was squash-merged with green hosted
  checks as `2bb9664bd02837f83b3fb0f9edfd1dfc237c6f49`; its remote branch was
  deleted.
- The annotated `v0.4.5` tag points to
  `2bb9664bd02837f83b3fb0f9edfd1dfc237c6f49`, which is reachable from
  `main`.
- The tag workflow
  [31771937355](https://github.com/hishamkaram/claude-code-router/actions/runs/31771937355)
  passed its maintainability, vet, ancestry, lint, race, vulnerability,
  GoReleaser, release, attestation, image publication, and image-signing
  steps.
- The published release
  [v0.4.5](https://github.com/hishamkaram/claude-code-router/releases/tag/v0.4.5)
  contains the four platform archives and `checksums.txt`; all four archive
  checksums pass. GitHub artifact attestation verification passed for every
  archive with the release workflow signer.
- The versioned GHCR browser image and `browser:latest` both resolve to
  `sha256:adb59577caf30d1598f947b3ee37a097302d96fa461b98347366bca7493bf861`.
  Cosign v3.0.6 verifies that digest with the expected
  `release.yml@refs/tags/v0.4.5` certificate identity and the GitHub Actions
  OIDC issuer.
- The manual promotion workflow
  [31772403690](https://github.com/hishamkaram/claude-code-router/actions/runs/31772403690)
  passed its draft-release/image preflight, `browser:latest` promotion,
  GitHub Release publication, and Homebrew publication steps.
- Homebrew tap commit
  [`fd828d60`](https://github.com/hishamkaram/homebrew-tap/commit/fd828d60fb676a53349dadb85160763f3e04507c)
  updates the formula to v0.4.5. Its codeload source archive SHA256 is
  `47ded5b1e374196c9509323a6c1049bf93e6e6f797e9cacbcc1b9722ab2306c4`,
  matching the formula.
