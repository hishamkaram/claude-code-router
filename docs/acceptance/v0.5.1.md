# CCR v0.5.1 Acceptance

Scope: wait for killed process-group members to exit before reporting cleanup
survivors. Cleanup remains partial: escaped descendants cannot be excluded.
There are no CLI, database, dependency, or signal-target changes.

## Fix verification

Source: `a837f8bdd27f586e0a808ba1fc48c502ac443525`, tree
`717fa60815d5739b33991caa7bbd2344cff1d49f`.
[Fix PR](https://github.com/hishamkaram/claude-code-router/pull/78).
Evidence date: 2026-09-11, macOS arm64, Go 1.27.1, Claude Code 2.1.268.

- [x] `go build ./...`, `go test -count=1 ./...`,
  `go test -race -count=1 -p 4 ./...`, `go vet ./...`,
  `golangci-lint run ./...`, `govulncheck ./...`, and
  `go run ./tools/maintainability` passed. Standard lint reported zero issues;
  maintainability reported no hard violations and 71 existing target warnings.
- [x] Full live suite: 1,364 tests passed, none failed, 16 environment-specific
  opt-in tests skipped. All detached-job acceptance tests ran.
- [x] Five repetitions of `TestLiveDetachedClaudeJobs` passed against real Claude.
- [x] Direct built-CLI proof launched two real Claude jobs against a local provider
  fixture. Cancelling one reached cancelled with no observed survivors while the
  sibling remained running. Cancelling the sibling also left no observed survivors.
- [x] Linux arm64 process-group fallback and observation tests passed 20 repetitions
  in Docker (140 top-level passes). This does not claim systemd-scope coverage.
- [x] Negative controls failed as expected when polling was replaced by one
  observation and when observation occurred before SIGKILL.
- [x] Independent code review and Codex companion review each reported zero
  findings and zero questions on the source tree above.

The full live command was:

```sh
env -u CLAUDE_CONFIG_DIR \
  CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1 \
  CLAUDE_CODE_DISABLE_OFFICIAL_MARKETPLACE_AUTOINSTALL=1 \
  CLAUDE_CODE_DISABLE_AUTO_MEMORY=1 \
  go test -tags=live -count=1 -p 1 -json ./...
```

These environment settings avoid ambient Claude traffic and preserve tests'
private HOME-based cache paths. Earlier runs with ambient traffic or a global
CLAUDE_CONFIG_DIR failed; no assertion or protocol scope was weakened.
Local-provider fixtures do not establish external-provider connectivity.
Optional live-tag lint exposed 15 pre-existing findings in unchanged files;
the required standard lint and live-tag lint restricted to new changes passed.

Review evidence: Codex companion job `task-mtwe8658-8kdxsz`, session
`01a08e7f-70c3-7f12-a037-f7effe3c2a90`; post-join gate passed.

## Release gates

- [x] macOS helper protocol suite passed (8 tests).
- [x] Native macOS helper compiled successfully.
- [x] Docker browser fixture: `make test-cua-docker-fixture` passed (5.160s).
- [x] Configured real-provider aggregate passed as the real stage of the
  combined gate (600.100s), with all 43 configured aliases included.
- [x] Combined fixture and configured real-provider matrix passed
  (fixtures 208.744s; real providers 600.100s).
- [x] `goreleaser check` and `goreleaser release --snapshot --clean` passed;
  all four archive checksums verified. `actionlint` passed.
- [x] All 19 fix-PR checks passed, including the 12-way protocol matrix and
  four durable-job checks. PR #78 squash-merged as
  `9e9642136e7bbc0538b4102110326c1275d576a0`; merged tree exactly matches
  the reviewed source tree above.
  [CI](https://github.com/hishamkaram/claude-code-router/actions/runs/34560430928),
  [live E2E](https://github.com/hishamkaram/claude-code-router/actions/runs/34560430953).
- [ ] Tag on approved main; draft archives, checksums, and provenance.
- [ ] Signed browser image and Linux/macOS archive smoke tests.
- [ ] Promotion, browser latest, and matching Homebrew formula.

Unchecked release gates are pending, not passes. The macOS CUA helper remains
an unsigned, source-built preview and is not included in release archives.

## Release preparation results

`make test-live-subscription-pool-fixture` passed (8.485s), followed by
`make test-live-fixture` (200.666s), with the Claude environment above.

The first configured real aggregate failed (471.444s): the full model-switch
matrix passed, but the idle-turn, Agent/WebFetch, and Workflow probes used
their default `glm-5-2` alias, absent from this machine's registry. Subsequent
runs set the supported `CCR_LIVE_CONFIGURED_MODEL_ALIAS=litellm-glm-5-2`.
No configured alias is excluded from v0.5.1 testing. This configuration
correction does not change product code or test assertions.

On clean, synchronized main `9e9642136e7bbc0538b4102110326c1275d576a0`,
`make check` passed. The full live suite passed again with 1,364 passes,
zero failures, and 16 opt-in skips. GoReleaser configuration, all four fresh
snapshot archives, and their checksums passed. The release documentation
changes do not alter the tested runtime source.

The configured rerun failed (500.671s): the idle-turn, Agent/WebFetch, and
Workflow probes passed, but `litellm-qwen3-7-plus-1m` returned 33 bytes instead
of the expected `CCR_LIVE_REAL_ALIAS_40` marker. Every other alias turn and
first-party return passed. Attribution is not established; no provider output
is normalized and no alias is excluded. The combined matrix reruns all
assertions against the unchanged runtime source.

The saved Claude session `fe454606-4e11-4337-87d7-d9790d2287f0` establishes
the failing order: alias marker requested at 04:14:04.021 UTC; Qwen returned
`CCR_LIVE_REAL_ANTHROPIC_RETURN_40` at 04:14:14.503; the test requested that
return marker only at 04:14:14.625. Launch 234's provider route completed with
HTTP 200 on the intended alias in 10.412s. This rules out early submission
of the next test prompt, but the raw upstream response was not retained, so
it does not distinguish model, LiteLLM, and translation behavior.

The unchanged combined rerun passed with all 43 configured aliases, every
first-party return, idle-turn, Agent/WebFetch, and Workflow checks. The Qwen
turn in session `57935915-a73f-44ea-a882-56dc0084bc79` returned the exact
22-byte `CCR_LIVE_REAL_ALIAS_40` at 04:25:35.886 UTC. This successful rerun
establishes intermittent behavior, not a fix or attribution for the prior
response mismatch. Grok 4.6 was included and passed in this release matrix.

```sh
env -u CLAUDE_CONFIG_DIR \
  CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1 \
  CLAUDE_CODE_DISABLE_OFFICIAL_MARKETPLACE_AUTOINSTALL=1 \
  CLAUDE_CODE_DISABLE_AUTO_MEMORY=1 \
  CCR_LIVE_REAL_MATRIX=1 \
  CCR_LIVE_CONFIGURED_MODEL_ALIAS=litellm-glm-5-2 \
  make test-live-matrix
```

The command runs the full `test-live-real` aggregate after fixtures. The
standalone configured attempt and its failure remain recorded above. Release
documentation review and CI must pass before tagging.
