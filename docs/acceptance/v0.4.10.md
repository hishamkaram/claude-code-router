# CCR v0.4.10 Acceptance Criteria

This release makes provider streaming and Claude Code compaction reliable without
client timeout workarounds. A checked item requires reproducible evidence on the
final release candidate; skipped live tests are not evidence.

## Behavior

- [x] Streamed OpenAI Chat Completions, OpenAI Responses,
  Anthropic-compatible, and managed computer-use provider traffic is translated
  incrementally into Anthropic SSE.
- [x] Each translated stream starts with a unique CCR message ID and a bounded
  input-token estimate suitable for Claude Code compaction accounting.
- [x] CRLF/LF SSE framing, delayed native `message_start`, terminal finish
  reasons, usage, text deltas, tool input, and malformed provider frames have
  explicit protocol handling.
- [x] Stream protocol, authentication, and tool-argument failures stay visible
  in-band with redacted errors; no silent provider or Claude fallback occurs.
- [x] Claude Code manual `/compact` and `--autocompact 100k` retain session
  state, reduce the continuation payload, and complete a post-compaction turn
  through a streaming-only OpenAI-compatible provider.

## Privacy and Security

- [x] No raw provider secret, authorization header, prompt, response, or tool
  argument is stored or printed by the new streaming and telemetry paths.

## Required Verification

- [x] `make check`
- [x] `make test-cua-docker-fixture`
- [x] `make test-live-subscription-pool-fixture`
- [x] `make test-live-fixture`
- [x] `CCR_LIVE_REAL_MATRIX=1 make test-live-real`
- [x] `CCR_LIVE_REAL_MATRIX=1 make test-live-matrix`
- [x] `go test -tags=live -count=1 -p 1 ./...`
- [x] `goreleaser check`
- [x] `goreleaser release --snapshot --clean`
- [x] `actionlint`
- [x] `git diff --check`
- [x] `make test-cua-macos-fixture` passes in the hosted macOS helper job.
- [x] The exact uncommitted Codex review reports no blocking correctness
  findings after the final release-candidate edit.
- [x] Hosted pull-request CI and the required live Claude Code E2E matrix pass
  without required-job skips.

## Release

- [ ] The complete patch is squash-merged through a reviewed PR with green CI
  and the feature branch is deleted.
- [ ] Annotated tag `v0.4.10` points to the approved commit reachable from
  `main`.
- [ ] Draft release archives, checksums, provenance, and published artifacts
  are verified.
- [ ] Manual promotion publishes the GitHub Release and updates Homebrew.

## Evidence

- Date: 2026-09-05
- Claude Code: 2.1.260 on the local verification host.
- Commands:
  - `make check` passed with zero hard maintainability violations, zero lint
    issues, a clean complete race suite, and no known vulnerabilities.
  - `make test-cua-docker-fixture` passed the browser entrypoint checks and
    real Docker screenshot smoke.
  - The subscription-pool fixture, full fixture suite, configured real-provider
    matrix, and combined fixture-plus-real matrix all passed without skips.
  - The full live-tag suite passed; `internal/cli` completed in 351.187 seconds
    and `internal/gateway` in 17.566 seconds.
  - `goreleaser check`, the Linux/macOS amd64/arm64 snapshot release,
    `actionlint`, and `git diff --check` passed.
  - `codex review -c 'sandbox_mode="danger-full-access"' -c
    'approval_policy="never"' -c
    'mcp_servers.playwright.enabled=false' -c
    'mcp_servers.chrome-devtools.enabled=false' --uncommitted` reported no
    blocking correctness findings. Its independent `go test ./...`, `go vet`,
    `golangci-lint`, and focused gateway race checks also passed.
- CI: [PR #63 Test](https://github.com/hishamkaram/claude-code-router/actions/runs/33977078522)
  passed in 2m30s. [PR #63 Live Claude Code E2E](https://github.com/hishamkaram/claude-code-router/actions/runs/33977078466)
  passed all 12 macOS/Ubuntu, pinned/latest CLI, and Anthropic/OpenAI fixture
  combinations, plus the Docker CUA browser fixture and hosted macOS CUA helper;
  no required job was skipped.
- Draft release: Pending tagged-workflow evidence.
- Promotion: Pending manual-promotion evidence.
