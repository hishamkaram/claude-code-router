# CCR v0.4.6 Acceptance Criteria

This release fixes image-bearing Anthropic `tool_result` content on
OpenAI-compatible Chat Completions routes. A checked item requires reproducible
evidence on the final release candidate; skipped live tests are not evidence.

## Behavior

- [x] Nested image blocks in Anthropic `tool_result` content are converted to
  OpenAI `image_url` parts instead of returning the previous 501 rejection.
- [x] Tool-result text remains in a contiguous OpenAI `tool` message sequence.
- [x] Image-only tool results retain a non-empty `[image output]` placeholder.
- [x] Extracted images are emitted in a trailing user message after all tool
  messages, preserving image and text order.
- [x] Existing image source validation, size limits, URL restrictions, and
  vision capability validation remain enforced.
- [x] A live Claude Code MCP image-tool test proves the behavior after an
  in-session `/model` switch to an OpenAI-compatible alias.
- [x] The live path produces the expected response and no 501 compatibility
  error.

## Privacy and Security

- [x] Image conversion reuses the existing resolver and does not log or persist
  image contents.
- [x] No provider secret handling, persistence, or authentication path changed.

## Required Verification

- [x] `GOTOOLCHAIN=go1.25.13+auto make check`
- [x] `GOTOOLCHAIN=go1.25.13+auto go test -tags=live -count=1 -p 1 ./...`
- [x] `git diff --check`
- [x] Focused live model-switch image-tool test passes with the installed
  Claude Code CLI.
- [x] The exact uncommitted Codex review reports no findings:

  ```bash
  codex review -c 'sandbox_mode="danger-full-access"' \
    -c 'approval_policy="never"' \
    -c 'mcp_servers.playwright.enabled=false' \
    -c 'mcp_servers.chrome-devtools.enabled=false' \
    --uncommitted
  ```

- [x] `goreleaser check`
- [x] `goreleaser release --snapshot --clean`
- [x] `actionlint`
- [ ] Hosted pull-request CI and the Live Claude Code E2E workflows pass with
  no required-job skips.

## Release

- [ ] The complete patch is squash-merged through the reviewed PR with green
  CI and the feature branch is deleted.
- [ ] Annotated tag `v0.4.6` points to the approved commit reachable from
  `main`.
- [ ] Draft release archives, checksums, provenance, and the signed GHCR
  browser image are verified.
- [ ] Manual promotion publishes the GitHub Release and updates Homebrew.

## Evidence

Verified locally on 2026-08-19 for the release candidate:

- Gateway unit tests, route-fixture tests, race tests, vet, lint, and
  vulnerability scanning passed with zero hard maintainability violations.
- The full tagged live suite passed, including the live Claude Code MCP image
  tool-result model-switch test.
- GoReleaser configuration and snapshot packaging passed for Linux and macOS
  amd64/arm64 archives; the browser Docker fixture and macOS protocol fixture
  also passed.
- The selected hosted-style live fixture matrix passed with the installed
  Claude Code CLI.
- The exact requested Codex review completed with no findings after the live
  replay passed.
- Hosted CI, release-tag workflow, artifact verification, and promotion
  evidence remain pending until the reviewed PR and release workflow complete.
