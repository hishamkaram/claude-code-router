# CCR v0.5.0 Acceptance

Scope: publish CCR's durable job API. Separate runner migration is not included.
Checked items require reproducible evidence. Skipped tests are not passes.

## Local Feature Evidence

Evidence date: 2026-09-10. Detailed commands, host evidence, review findings,
and resolutions are in [durable jobs acceptance](durable-jobs.md).

- [x] Durable detached admission, status, and idempotent job-ID cancellation.
- [x] Linux scope containment, sibling preservation, missing-scope safety,
  admission races, external-unit escape, and mandatory mutation controls.
- [x] Bounded direct-child migration and lost-bus cleanup without signal fallback.
- [x] Actual Claude CLI completion, cancellation, stream JSON, and argument validation.
- [x] Full unit, race, vet, lint, vulnerability, maintainability, and live gates.
- [x] Uncommitted review completed with no actionable defects remaining.
- [x] macOS and Windows cross-builds.
- [x] PR CI, including Linux/macOS and pinned/latest Claude job tests.

## Release Preparation

- [x] Release configuration and snapshot archives validated.
- [x] macOS helper protocol and Docker browser fixture evidence recorded.
- [x] Live fixture and subscription-pool fixture evidence recorded.
- [x] Configured real-provider matrix evidence recorded.
- [x] Feature PR squash-merged into main; feature branch deleted.

## Publication

- [ ] Version tag references approved main commit.
- [ ] Draft includes Linux/macOS amd64/arm64 archives and checksums.
- [ ] Archive checksums and GitHub provenance verified.
- [ ] Signed GHCR browser image verified.
- [ ] Linux and macOS archive smoke tests passed.
- [ ] Promotion publishes GitHub Release and updates browser latest.
- [ ] Homebrew formula references the tag with matching source checksum.

## Known Limits

Native cleanup does not exclude every escape. Owner loss can leave work for
operator attention. Job logs are private but can contain sensitive content.
Real paid-provider connectivity is not established by the local provider fixtures.
The macOS CUA helper is source-built only and remains an unsigned preview.

## Publication Evidence

Pending. Append run URLs, commit/tag identities, and verification results here.

## Release Preparation Evidence

On 2026-09-10, `make check`, `make test-cua-macos-fixture`, and
`make test-cua-docker-fixture` passed on the Linux VPS. The macOS helper result
is its portable protocol suite, not a native macOS build.
`goreleaser check`, `goreleaser release --snapshot --clean`, `actionlint`, and
`shasum -a 256 --check checksums.txt` in the snapshot directory passed.
Snapshot archives were produced for all four Linux/macOS architecture targets.
The release workflow now smoke-tests downloaded draft archives and verifies
their checksums and provenance on Linux and macOS before promotion.

`make test-live-fixture` passed (301.823s). The subscription-pool fixture
initially hit two timeouts during concurrent validation. Both failed cases
passed in isolation (7.261s), then the entire
`make test-live-subscription-pool-fixture` passed (25.499s).

The first PR CI run exposed Darwin's zombie-only process-group `EPERM`
behavior and a Linux migration test that ran before the user manager was
ready. Empty-group error handling now requires a successful observation with
no live members; actual permission failures remain errors. Linux CI checks
the user manager through D-Bus before running containment tests, and the
migration test explicitly requires a scope. Linux live job tests, including
mutation controls, passed after these changes (20.642s); actual Claude
detached-job lifecycle passed (11.680s). Native macOS CI confirmation is pending.

In [the next CI run](https://github.com/hishamkaram/claude-code-router/actions/runs/34476199971),
both macOS durable-job checks passed, including actual Claude completion and
cancellation. The explicit Linux scope probe exposed `NameHasNoOwner` on the
session bus. `systemctl --user` readiness was insufficient because it can use
systemd's private socket. CI now waits, bounded to 30 seconds, for
`busctl --user status org.freedesktop.systemd1` on the same session bus CCR uses.
No containment test is skipped or weakened.

The correction review found no actionable regressions. Its concurrent race
run hit the unchanged gateway idle-ping timing test; ten isolated repetitions
passed. The separate full `make check` run passed, including its race suite.

The initial configured real-provider run failed one exact response-marker
assertion for `litellm-grok-4-6` after completing the surrounding model-switch
turns. A full matrix rerun is required; this is not recorded as a pass.

All 19 checks passed for `0e7e8b6`, including all four native durable-job
checks and the 12-way protocol fixture matrix:
[CI](https://github.com/hishamkaram/claude-code-router/actions/runs/34477128950),
[live E2E](https://github.com/hishamkaram/claude-code-router/actions/runs/34477128929).

The second real-provider run passed Grok 4.6 but rejected model selection for
`litellm-glm-4-7` and `litellm-glm-5v-turbo-openrouter`. Its workflow assertion
also selected another concurrently running matrix's newer database record.
The release harness now captures the workflow's actual launch ID and reads
that record directly, instead of assuming the newest row belongs to the test.

The corrected workflow test passed in isolation (28.984s) and during a live
overlap control (40.033s). Workflow launch 255 ran from 12:39:47 to 12:40:27
UTC; unrelated provider launch 256 ran from 12:40:07 to 12:40:16 UTC and
returned `CCR_RELEASE_IDENTITY_CONTROL`. The workflow still verified its own
agent lifecycle and route evidence. No provider/model assertion was relaxed.

The full live suite passed after the native cleanup changes (CLI 394.560s,
jobs 14.888s). Fresh four-platform snapshot archives and all checksums passed.
The combined `CCR_LIVE_REAL_MATRIX=1 make test-live-matrix` fixture stage
passed (300.871s), but its real-provider stage failed (689.309s). The workflow
and the previously rejected GLM selections passed; Grok 4.6 again returned
44 bytes instead of the 22-byte marker. The saved Claude transcript for that
test confirms the exact duplicate
`CCR_LIVE_REAL_ALIAS_33CCR_LIVE_REAL_ALIAS_33`. A fresh direct single-turn CLI
probe returned the marker once. The source of this intermittent duplication
has not been established; neither CCR nor the upstream provider is exonerated.

**Initial publication block:** the required configured real-provider matrix had no
clean aggregate pass. Do not tag or promote v0.5.0 based on green fixture CI
alone. The durable-job feature and the workflow evidence correction can merge
after their final PR checks pass; paid-provider verification remains a separate
release blocker. No output normalization or weakened marker assertion is used.

## Issue 72 Recheck

[PR #71](https://github.com/hishamkaram/claude-code-router/pull/71) was
squash-merged as `220bf1bad29850b98cf664e12ff5b154a90870dd`; its feature branch
was deleted after all 19 checks passed. The remaining investigation is tracked
in [issue #72](https://github.com/hishamkaram/claude-code-router/issues/72).

On 2026-09-10, the complete 44-alias same-session test passed in 736.9s.
Temporary bounded tracing compared OpenAI content deltas with translated
Anthropic text. The Grok turn contained exactly 22 bytes and one marker at
both boundaries, with matching SHA-256
`19276eca85da22c4395b62cca417afef596aeaaffd4837446bcafdbbdd9459b3`.
Claude returned the same single marker. Eight forked long-session probes
passed (124.07s), followed by eight probes forked before the historical
failure (119.85s). These forks preserved the original saved transcript.
The diagnostic-only code was removed before release validation.

`go test ./internal/gateway -run '^TestOpenAIStreamTextFidelity$' -count=1`
passes for single, split, empty, and intentionally repeated text deltas.
Ten race-detector repetitions also passed. A deliberate duplication mutation
failed all five cases; a deduplication mutation failed the repeated-delta case.
Both mutations were removed. These tests protect stream fidelity; they do
not claim to reproduce or fix the historical Grok failure.

Fresh `go test ./...` and `make check` passed, including zero lint issues,
the full race suite, vet, and vulnerability checks. The macOS protocol suite
passed all eight tests; the Docker screenshot fixture passed (8.027s).
The subscription-pool fixture passed (36.774s), and the full Claude fixture
suite passed (313.524s). Four snapshot archives and their checksums passed;
GoReleaser configuration and Actions workflow validation passed.

The full `CCR_LIVE_REAL_MATRIX=1 make test-live-real` aggregate passed
(819.861s), including all aliases, first-party returns, agent WebFetch, and
workflow lifecycle evidence. The second long session also saved one Grok marker.

The historical duplication remains unexplained and issue #72 remains open.
No text normalization, model exclusion, or weakened exact-marker assertion is
part of this recheck. The combined matrix, final review, and PR CI remain
required before publication.

## Idle Pool Correction

The full live suite passed (CLI 384.308s, jobs 14.922s). The next combined gate
passed its fixture stage (289.990s), but the real stage failed (857.670s):
Grok and every other tested alias passed except the Haiku 4.5 selection.
That request returned `http2: client connection lost`. Its preceding provider
route completed at 13:53:13.674568064 UTC; the Haiku request began at
13:53:48.134298708 and failed at 13:53:48.677721476, 35.003 seconds after
the prior route completed. A fresh actual CLI probe returned
`CCR_RELEASE_HAIKU_CHECK`. This is tracked separately in
[issue #73](https://github.com/hishamkaram/claude-code-router/issues/73).

A deterministic HTTP/2 fixture reproduced reuse of a connection whose idle
health probe had not been acknowledged. The existing 90-second pool policy
failed with the same connection-lost error. The correction caps gateway-owned
idle connections at 15 seconds, before the 20-second health probe begins,
while preserving shorter caller limits and active-stream pings. Caller-owned
clients are unchanged. This uses Go's standard
[idle connection policy](https://pkg.go.dev/net/http#Transport), not retries
or protocol fallback. The missing ACK's external cause is not established.

The corrected regression and its eviction-disabled negative control passed
ten race-detector repetitions (15.582s). Both controls assert exactly two
accepted POSTs, so replay cannot satisfy the test. Existing active-stream
liveness, deadlines, missing-ACK, multiplexed cancellation, TLS, and proxy
tests passed under the race detector (6.279s).

The actual Claude CLI idle-turn probe passed (47.842s). It uses bare mode for
a focused transport test, accepts Claude's startup probes, verifies a real
quiet interval from persisted route timestamps, and requires both exact
user-turn markers with no failed or fallback routes. The broad matrix retains
normal Claude mode. This probe is now included in the real-provider gate.
Final complete gates and review must pass on the corrected source before tagging.

Review found a synchronization race in the new idle-pool fixture: a trailing
response frame could be mistaken for the health probe. The fixture now waits
for actual connection closure within a bounded interval before probe failure;
the eviction-disabled control must remain open through that interval. One
hundred race repetitions passed (146.044s). Removing the production idle cap
through a temporary Go overlay failed all three mutation repetitions at the
closure deadline (2.229s). The overlay did not modify the release source.

The corrected source passed `go test ./...` and a fresh `make check` (CLI race
106.388s, gateway race 123.470s, zero lint issues, no vulnerabilities). The
full live suite passed (CLI 372.487s, jobs 14.620s). The macOS protocol suite,
Docker fixture (17.510s), subscription-pool fixture (23.078s), and full Claude
fixture (306.166s) passed. Fresh four-platform snapshot checksums,
GoReleaser configuration, and Actions validation passed.

The next real aggregate failed (927.484s): the idle-turn probe, Grok, Haiku,
agent workflow, and other selected aliases passed, but Claude's GLM 4.7
availability check timed out after 15 seconds. The gateway route succeeded
after 20.683 seconds. Session `bbec2c30-44c8-42f1-a836-f9da6e471ee1`, launch
297, records the native timeout at 14:39:27.812 UTC and route completion at
14:39:33.498616205. This is not a passing aggregate and is not established as
an idle-pool failure. The combined matrix remains a publication gate.

## Final Release Block

The next combined fixture stage failed (277.678s): its Anthropic-native
conformance assertion observed one first-party classifier request. Ten focused
repetitions passed (35.223s), followed by 50 instrumented repetitions (181.481s).
No assertion was changed. This unresolved observation is tracked in
[issue #75](https://github.com/hishamkaram/claude-code-router/issues/75).

The following combined run passed fixtures (290.904s), but its real-provider
stage failed (925.173s). All other alias turns, the idle-turn probe,
Agent/WebFetch, and Workflow passed. Grok 4.6 returned the 33-byte marker
`CCR_LIVE_REAL_ANTHROPIC_RETURN_33` instead of the requested 22-byte marker
`CCR_LIVE_REAL_ALIAS_33`. This is a wrong-marker response, not the original
exact duplication. Session `5831c6d2-a194-4ee8-8368-ae5fdb2a47b2`, launch 301,
records the alias-marker prompt at 15:11:25.751 UTC and Grok response at
15:11:34.451. The subsequent first-party-marker prompt was sent only at
15:11:36.091. The receipt for provider call
`d136743c-c63d-4480-b934-5d877e7370b8` reports zero retries and fallbacks.
The failing upstream body was not retained, so attribution is not established.

Twelve forked probes resumed before that turn passed (160.447s). Temporary
bounded instrumentation found the correct latest user marker and exactly 22
upstream text bytes, matching the earlier text hash, in every probe; Claude
also returned the correct marker. These provider-only resumed probes are not
equivalent to the original normal-mode matrix and do not explain its failure.
The Go overlay and all diagnostic source lived outside the release tree.

The corrected implementation review found no actionable defects. It reran
unit tests, `make check`, and ten focused race repetitions successfully.
All 19 PR checks passed for `50dd16367c53a309c20a84dcfe8168236865c407`:
[CI](https://github.com/hishamkaram/claude-code-router/actions/runs/34492176905),
[live E2E](https://github.com/hishamkaram/claude-code-router/actions/runs/34492176863).
This includes all four durable-job checks and the 12-way protocol matrix.
Fresh snapshot archives for that commit and all four checksums also passed.

**Publication remains blocked.** [PR #74](https://github.com/hishamkaram/claude-code-router/pull/74)
contains the independently verified idle-pool correction and stream-fidelity
tests, not a fix for Grok's response failures. No v0.5.0 tag, draft, promotion,
or Homebrew update has been made. Green fixture CI does not replace the
required complete real-provider gate; issue #72 remains open.

## Owner-Approved Scope Change

On 2026-09-10, the owner explicitly requested skipping Grok 4.6. Only
`litellm-grok-4-6` is excluded from v0.5.0 real-provider acceptance. This
supersedes the Grok-only publication block above, not the remaining gates.
Grok 4.6 remains unverified for this release and issue #72 stays open.

Validation uses a private SQLite backup of the configured database at
`/tmp/ccr-v0.5.0-gate.Z0dFU8/ccr.db`, with only that alias's status changed
to `blocked`. The original configuration is unchanged. The snapshot has all
44 original aliases, with 43 non-blocked; comparing statuses confirms exactly
one difference. No production behavior, output assertion, or permanent test
exclusion was changed. Grok 4.5 remains included.

The scoped combined gate is:

```bash
CCR_LIVE_REAL_MATRIX=1 \
CCR_LIVE_CONFIGURED_DB=/tmp/ccr-v0.5.0-gate.Z0dFU8/ccr.db \
make test-live-matrix
```

The 43-alias result, final review, and PR CI must pass before tagging.

The first scoped combined attempt stopped in its fixture stage (457.397s),
before paid-provider testing: the interactive all-accounts-limited fixture
timed out waiting for its first request. Three isolated repetitions passed
(14.218s). The fixture's quiet-terminal heuristic does not establish input
readiness. Both continuity fixtures now provide the same prompt as an initial
Claude argument and wait for real upstream requests, instead of typing after
an assumed-ready interval. They remain interactive and retain their process
continuity, account routing, and secret-isolation assertions. No production
code changed for this correction.

Ten actual-Claude repetitions of the all-accounts-limited case passed
(38.330s), and ten rotation-without-restart repetitions passed (36.313s).
This removes the readiness assumption; the original timeout did not preserve
enough terminal evidence to establish why no request arrived.

## Scoped Release Verification

On source commit `483df1f4cdebfd75cab6d024ffaaa05bb76acad0`, `make check`
passed, including race, vet, lint, vulnerability, and maintainability checks.
The full `go test -tags=live -count=1 -p 1 ./...` passed (CLI 416.878s,
jobs 15.058s). The requested review reported no actionable regressions; it
independently passed unit tests, `make check`, and three actual-Claude
repetitions of each corrected interactive subscription-pool case.

The scoped `make test-live-real` passed in 869.037s with the private database
and environment above. This includes the 43-alias same-session matrix,
idle-turn probe, Agent/WebFetch, and Workflow. Only Grok 4.6 was excluded.
The combined gate's fixture stage passed in 269.489s, but its real-provider
stage failed in 855.658s. Claude rejected only `litellm-glm-4-7-openrouter`:
the saved session `65357414-67bd-4e85-b2c5-fd76ae3f3739` records its native
availability-check timeout at 16:08:39.823 UTC, 15 seconds after selection.
Launch 321's corresponding route completed with HTTP 502 after 18.460 seconds.
All other alias turns, idle-turn, Agent/WebFetch, and Workflow passed. This
is not a passing combined result, and no additional alias is excluded.

Three focused actual-Claude GLM OpenRouter selection/turn/return repetitions
passed (89.746s), using a separate diagnostic database and the existing
same-session matrix test. That diagnostic scope is not release acceptance.
The full combined gate is rerun against the unchanged 43-alias release database.

That unchanged combined rerun passed: fixtures in 305.365s and the complete
real-provider stage in 909.162s. All 43 included alias turns and first-party
returns, idle-turn, Agent/WebFetch, and Workflow passed. Launch 325's GLM
OpenRouter selection completed in 1.753s and its streamed turn in 2.997s.
The earlier timeout is retained above; a successful rerun is not a claimed
fix for intermittent provider availability. No output normalization, assertion
relaxation, or additional exclusion was used.

All 19 PR checks passed for that source commit:
[CI](https://github.com/hishamkaram/claude-code-router/actions/runs/34498540443),
[live E2E](https://github.com/hishamkaram/claude-code-router/actions/runs/34498540339).
Fresh four-platform snapshot archives, all four checksums, `goreleaser check`,
and `actionlint` passed. The scoped pre-tag live gates are complete; final
acceptance review and green CI on the evidence commit are required before
merging and tagging. Grok 4.6 remains explicitly outside acceptance.
